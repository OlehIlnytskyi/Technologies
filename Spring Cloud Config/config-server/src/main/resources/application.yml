
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/OlehIlnytskyi/Technologies
          search-paths: 'Spring Cloud Config/configs/{application}'
          repos: {label}

vars:
  variable_one:
    value: inside-config-server